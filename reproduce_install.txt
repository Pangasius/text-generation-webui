# for unstructured, deepspeed
sudo apt-get install libreoffice libaio-dev

# conda torch
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 cuda-nvcc -c pytorch -c nvidia

# conda special that often have conflicts so do it alone
conda install -c conda-forge mpi4py mpich

# conda other
conda install -c conda-forge regex pytesseract markdown transformers accelerate datasets scipy sentencepiece protobuf sentence-transformers colorama einops optimum tensorboard wandb scikit-learn-intelex xformers

# not on conda
pip install llama-index llama-hub pdf2docx peft bitsandbytes "unstructured[all-docs]" nebula3-python exllamav2 deepspeed gradio auto-gptq torch_grammar

# install postgres db
# install https://github.com/pgvector/pgvector
pip install psycopg2-binary pgvector asyncpg "sqlalchemy[asyncio]" greenlet